syntax = "proto3";

package io.ontherocks.hellogrpc;

service Clock {
  // returns the current time (in ms) every second
  rpc GetTime(TimeRequest) returns (stream TimeResponse) {}
}

message TimeRequest {}

message TimeResponse {
  int64 currentTime = 1;
}